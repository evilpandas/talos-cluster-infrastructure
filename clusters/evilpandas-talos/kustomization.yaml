apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Our infrastructure
  - infrastructure.yaml

  # Our tenant applications
  - tenants.yaml

# Note: flux-system is managed separately by Flux itself
# Don't include it here as it creates circular dependencies
