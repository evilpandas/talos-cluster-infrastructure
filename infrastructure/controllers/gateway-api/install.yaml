---
# Gateway API CRDs - Installed manually via kubectl
# kubectl apply -f https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.2.0/standard-install.yaml
#
# Note: These CRDs are already installed in the cluster.
# This file just creates the namespace for organizational purposes.

apiVersion: v1
kind: Namespace
metadata:
  name: gateway-system
  labels:
    app.kubernetes.io/name: gateway-api
