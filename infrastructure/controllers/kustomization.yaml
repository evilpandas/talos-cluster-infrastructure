apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Gateway API CRDs (install first)
  - gateway-api/

  # Core operators and controllers
  - cloudnative-pg/
  - ingress-nginx/
  - cloudflare-tunnel/

  # Add more controllers as needed:
  # - external-secrets/  # Optional - for external secret management
