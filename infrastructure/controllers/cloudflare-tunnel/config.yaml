---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  namespace: cloudflare-tunnel
data:
  config.yaml: |
    # Tunnel ID from credentials
    tunnel: 298df0fd-2215-4d2d-b5b0-9c54b0f06ae1
    credentials-file: /etc/cloudflared/creds/credentials.json

    # Metrics for monitoring
    metrics: 0.0.0.0:2000

    # No TLS verification for internal services (optional)
    # no-tls-verify: true

    # Ingress rules - route traffic to ingress-nginx
    ingress:
      # Evil Downloader
      - hostname: dlp.evilpandas.com
        service: http://ingress-nginx-controller.ingress-nginx.svc.cluster.local:80
        originRequest:
          noTLSVerify: true

      # Homepage
      - hostname: home.evilpandas.com
        service: http://ingress-nginx-controller.ingress-nginx.svc.cluster.local:80
        originRequest:
          noTLSVerify: true

      # Uptime Kuma
      - hostname: status.evilpandas.com
        service: http://ingress-nginx-controller.ingress-nginx.svc.cluster.local:80
        originRequest:
          noTLSVerify: true

      # Grafana (monitoring)
      - hostname: grafana.evilpandas.com
        service: http://ingress-nginx-controller.ingress-nginx.svc.cluster.local:80
        originRequest:
          noTLSVerify: true

      # Prometheus (monitoring)
      - hostname: prometheus.evilpandas.com
        service: http://ingress-nginx-controller.ingress-nginx.svc.cluster.local:80
        originRequest:
          noTLSVerify: true

      # Wildcard catch-all - returns 404
      - service: http_status:404
